services:
  - type: web
    name: telegram-grading-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py & python bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
